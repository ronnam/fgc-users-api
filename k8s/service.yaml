apiVersion: v1
kind: Service
metadata:
  name: fgc-users-api
  labels:
    app: fgc-users-api
spec:
  type: ClusterIP
  selector:
    app: fgc-users-api
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
